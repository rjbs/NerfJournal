import SwiftUI

@main
struct NerfJournalApp: App {
    var body: some Scene {
        WindowGroup {
            NavigationStack {
                ContentView()
            }
        }
        .defaultSize(width: 420, height: 640)
    }
}
